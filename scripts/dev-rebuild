#!/usr/bin/env bash
# Rebuild and restart teamarr (use when NOT using code volume mount)
set -e
cd "$(dirname "$0")/.."
echo "Building and starting teamarr..."
docker compose up -d --build
echo "Done. Logs: docker compose logs -f teamarr"
